import { InrAmountPipe } from './inr-amount.pipe';

describe('InrAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new InrAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
